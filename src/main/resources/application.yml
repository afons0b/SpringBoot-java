spring:
  profiles:
    active: mysql
  application:
    name: spring-boot-trench

  threads:
    virtual:
      enabled: true

database:
  mysql:
    url: localhost:3306
    username: afonsob
    password: neekisz1
